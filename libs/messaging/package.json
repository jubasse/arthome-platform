{
  "name": "@arthome-platform/messaging",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The messaging contract every service shares: the outbox, failure classification, retry and dead-lettering.",
  "license": "UNLICENSED",
  "exports": {
    ".": {
      "@arthome/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.mjs"
  },
  "dependencies": {
    "@arthome/core": "file:../../vendor/arthome-core-5c370f5d4d1a.tgz",
    "kafkajs": "2.2.4",
    "typeorm": "1.1.1",
    "uuid": "14.0.2"
  },
  "devDependencies": {
    "@arthome-platform/testing": "workspace:*"
  }
}
