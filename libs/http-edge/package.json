{
  "name": "@arthome-platform/http-edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The HTTP edge every service shares: the exception filter, the production guard, the traceparent parser.",
  "license": "UNLICENSED",
  "exports": {
    ".": {
      "@arthome/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@arthome-platform/messaging": "workspace:*",
    "@arthome/core": "file:../../vendor/arthome-core-5c370f5d4d1a.tgz",
    "@nestjs/common": "12.0.3",
    "@nestjs/core": "12.0.3",
    "@nestjs/platform-fastify": "12.0.3",
    "rxjs": "7.8.2",
    "zod": "4.6.5"
  }
}
